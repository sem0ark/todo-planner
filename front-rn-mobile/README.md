# Tempo Mobile

Expo React Native implementation of the live schedule widget. The repository and reducer are intentionally isolated so the mock repository can later be replaced by the API client.

```bash
pnpm install
pnpm start
```

Run `pnpm ios` or `pnpm android` for a device/simulator build.
